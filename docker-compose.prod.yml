services:
  backend:
    environment:
      DESK_MQTT_PUBLIC_URL: ${DESK_MQTT_PUBLIC_URL:-wss://monitor.danielalbertorosso.com.ar/mqtt}
    volumes:
      - ./contracts/config/platform.production.yaml:/config/platform.yaml:ro
